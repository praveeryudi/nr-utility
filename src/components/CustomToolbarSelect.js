import React from 'react';

const CustomToolbarSelect = (props) => {

    const handleClick = () => {
        console.log('click!', props.selectedRows)
    };

    return (
     <div></div>
    )

};

export default CustomToolbarSelect;